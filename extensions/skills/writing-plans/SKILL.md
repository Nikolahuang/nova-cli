---
name: writing-plans
description: 创建详细实施计划，每个任务可在 2-5 分钟内完成，包含确切的文件路径、完整代码和验证步骤。
author: obra/superpowers
version: 1.0.0
tags: [planning, implementation, tdd]
requiredTools: [read_file, write_file, edit_file]
---

# 编写计划 - 从设计到实施

## 概述
将批准的设计分解为详细的实施计划。每个任务都足够小（2-5 分钟），包含确切的文件路径、完整代码和验证步骤。

## 计划格式

```markdown
# 实施计划: [功能名称]

## 概述
[1-2 段简短描述]

## 技术上下文
- 语言: [语言和版本]
- 框架: [框架及版本]
- 关键依赖: [列出]

## 任务清单

### 任务 1: [简短描述]
**文件**: `path/to/file.ts`
**时间**: 2-5 分钟

**要编写的代码**:
```typescript
// 完整可运行的代码
```

**验证步骤**:
1. [具体验证命令或测试]
2. [预期输出]

---

### 任务 2: [简短描述]
...
```

## 编写指南

### 任务大小
- 每个任务 2-5 分钟
- 单一职责
- 明确的开始和结束

### 代码完整性
- 完整可运行的代码片段
- 包含必要的导入
- 包含类型定义

### 验证
- 具体的验证命令
- 预期的输出或行为
- 测试命令（如适用）

## TDD 集成

如果使用测试驱动开发：

```markdown
### 任务 1.1: 写失败的测试
**文件**: `path/to/file.test.ts`

```typescript
test('功能描述', () => {
  // 测试代码
});
```

**验证**: `npm test` → 测试应该失败

### 任务 1.2: 写最小实现
**文件**: `path/to/file.ts`

```typescript
// 最小通过代码
```

**验证**: `npm test` → 测试应该通过
```

## 关键原则

- **粒度适中** - 小到可以快速验证
- **完整性** - 包含所有必要信息
- **可验证** - 每步都有明确的验证方式
- **顺序合理** - 依赖关系清晰
- **YAGNI** - 只计划当前需要的功能
